{
  "$schema": "https://wails.io/schemas/config.v2.json",
  "name": "{{.ProjectName}}",
  "outputfilename": "{{.BinaryName}}",
  "frontend:install": "npm install",
  "frontend:build": "npm run build:prod",
  "frontend:dev:build": "npm run build",
  "frontend:dev:watcher": "npm run watch",
  "frontend:dev:serve": "npm run serve",
  "author": {
    "name": "{{.AuthorName}}",
    "email": "{{.AuthorEmail}}"
  }
}
